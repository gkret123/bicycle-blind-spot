[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bicycle-blind-spot"
version = "0.1.0"
description = "Bicycle Blind Spot System"
authors = [{name="Gabriel Kret"}]
requires-python = ">=3.11"

[tool.setuptools]
packages = ["src"]
